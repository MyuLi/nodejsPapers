extends layout

block content
  h1= title
  p Welcome to #[em 毕业设计系统].

  h1 Dynamic content

  if err
    p Error getting dynamic content.
  else
    p The system has the following record counts:

    ul
      li #[strong Books:] !{data.project_count}
      li #[strong Copies:] !{data.student_count}
      li #[strong Copies available:] !{data.pchoose_count}
      li #[strong Authors:] !{data.plast_count}
      li #[strong Genres:] !{data.task_count}